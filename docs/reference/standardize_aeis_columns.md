# Standardize AEIS column names to match TAPR format

Standardize AEIS column names to match TAPR format

## Usage

``` r
standardize_aeis_columns(df, dsname)
```

## Arguments

- df:

  Data frame with AEIS column names

- dsname:

  Dataset name to determine prefix

## Value

Data frame with standardized column names
