# Reshape wide format to long format with subject as rows

Reshape wide format to long format with subject as rows

## Usage

``` r
reshape_to_long_format(wide_data)
```

## Arguments

- wide_data:

  Wide format data frame with separate RLA and Math columns

## Value

Long format data frame with subject column
